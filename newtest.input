func isEven(i int) bool {
    var ret bool := false;
    if i >= 2 {
        var ret bool := true;
    } else {
        var half int := i * 2.0;
        var full int := 2;
        if full {
            var ret bool := true;
        }
    }
    return ret;
}

func main() {
	abc / 5 - 7;
    hallo(abc * (5 - 7) );
}